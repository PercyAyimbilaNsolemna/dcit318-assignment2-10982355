using System;

namespace ShapesProgram
{
    //Creates an abstract class Shape
    abstract class Shape
    {
        //Creates an abstract method GetArea
        internal protected abstract double GetArea();
    }
}
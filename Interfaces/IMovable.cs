using System;

namespace ImovableProgram
{
    //Creates an IMovable interface
    interface IMovable
    {
        //Defines an abstract class Move
        //The modifier is public by default
        void Move();
    }
}